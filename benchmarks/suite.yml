---
name: "Benchmarks"
description: "Performance benchmarks for partial rendering"

default: true

timings: true
default_iteration_seconds: 5

hint: |
  These specs test partial rendering performance.

required_features:
  - core

defaults:
  render_errors: false
  complexity: 1000
