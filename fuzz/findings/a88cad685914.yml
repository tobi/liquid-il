# Differential fuzzer finding -- see .goals/02-differential-fuzzer.md
---
name: fuzz_a88cad685914
_metadata:
  doc: fuzz/README or goal 02 doc (.goals/02-differential-fuzzer.md)
  hint: 'rule=output_mismatch detail={index: 0, ref_ctx: "<div>markup<", lil_ctx:
    "Liquid error"}'
  signature: 'output_mismatch::[{type: :case, whens: [{body: [{type: :for, var: "id",
    coll: {type: :range}, body: [{type: :unless, branches: [{cond: {type: :logical,
    op: "or", right: {type: :chain, base: "id", accessors: [{dot: "id"}]}}}]}]}]}]}]'
  seed: 427788210
  subprocess_confirmed: true
template: "{% case nil %}{% when nil %}{% for item in (nil..nil) %}{% unless nil or
  item.name %}{% endunless %}{% endfor %}{% endcase %}"
environment: {}
expected: ''
complexity: 100
_liquidil_actual: 'Liquid error (line 1): no implicit conversion of String into Integer'
