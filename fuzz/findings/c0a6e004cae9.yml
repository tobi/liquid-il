# Differential fuzzer finding -- see .goals/02-differential-fuzzer.md
---
name: fuzz_c0a6e004cae9
_metadata:
  doc: fuzz/README or goal 02 doc (.goals/02-differential-fuzzer.md)
  hint: 'rule=output_mismatch detail={index: 69, ref_ctx: "Liquid error: invalid in",
    lil_ctx: "Liquid error (line 5): i"}'
  signature: 'output_mismatch::[{type: :if, branches: [{cond: {type: :logical, op:
    "or", left: {type: :binop, op: "=="}}, body: [{type: :for, var: "id", coll: {type:
    :range}, body: [{type: :unless, branches: [{body: [{type: :cycle, values: [{type:
    :chain, accessors: [{bracket: {type: :chain, base: "id"}}]}]}]}]}]}]}]}]'
  seed: 1685100048
  subprocess_confirmed: true
template: "{% if nil == nil or nil %}{% for row in (nil..nil) %}{% unless nil %}{%
  cycle [names] %}{% endunless %}{% endfor %}{% endif %}"
environment:
  names: []
expected: ''
complexity: 100
_liquidil_actual: "[]"
