# Differential fuzzer finding -- see .goals/02-differential-fuzzer.md
---
name: fuzz_6b149e1229d5
_metadata:
  doc: fuzz/README or goal 02 doc (.goals/02-differential-fuzzer.md)
  hint: 'rule=output_mismatch detail={index: 0, ref_ctx: "Hello, World", lil_ctx:
    "Liquid error"}'
  signature: 'output_mismatch::[{type: :for, var: "id", coll: {type: :range}, body:
    [{type: :if, branches: [{cond: {type: :chain, base: "id"}, body: [{type: :assign,
    name: "id", expr: {type: :chain, base: "id", accessors: [{dot: "id"}]}}]}]}]}]'
  seed: 1522356006
  subprocess_confirmed: true
template: "{% for it in (nil..nil) %}{% if forloop %}{% assign items = it.title %}{%
  endif %}{% endfor %}"
environment: {}
expected: ''
complexity: 100
_liquidil_actual: 'Liquid error (line 1): no implicit conversion of String into Integer'
