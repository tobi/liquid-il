# Differential fuzzer finding -- see .goals/02-differential-fuzzer.md
---
name: fuzz_2222ff3fa000
_metadata:
  doc: fuzz/README or goal 02 doc (.goals/02-differential-fuzzer.md)
  hint: 'rule=output_mismatch detail={index: 16, ref_ctx: "y % percent\n\", c %}",
    lil_ctx: "y % percent\nLiquid error"}'
  signature: 'output_mismatch::[{type: :unless, branches: [{cond: {type: :binop, op:
    "!=", right: {type: :chain, base: "id"}}}], else_body: [{type: :case, whens: [{values:
    [{type: :lit, value: "String"}]}]}]}]'
  seed: 1345283639
  subprocess_confirmed: true
template: '{% unless nil != total %}{% else %}{% case nil %}{% when "{% not_a_tag
  %}" %}{% endcase %}{% endunless %}'
environment:
  total: true
expected: "\" %}"
complexity: 100
_liquidil_actual: ''
