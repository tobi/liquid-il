# Differential fuzzer finding -- see .goals/02-differential-fuzzer.md
---
name: fuzz_748187a025b0
_metadata:
  doc: fuzz/README or goal 02 doc (.goals/02-differential-fuzzer.md)
  hint: 'rule=output_mismatch detail={index: 36, ref_ctx: "Liquid error: divided by",
    lil_ctx: "Liquid error (line 7): d"}'
  signature: 'output_mismatch::[{type: :for, var: "id", coll: {type: :chain, base:
    "id"}, body: [{type: :if, branches: [{cond: {type: :binop, op: "contains", right:
    {type: :chain, accessors: [{bracket: {type: :chain, base: "id", accessors: [{dot:
    "id"}]}}]}}}]}]}]'
  seed: 838087336
  subprocess_confirmed: true
template: "{% for entry in items %}{% if nil contains [entry.count] %}{% endif %}{%
  endfor %}"
environment:
  items:
  - true
expected: ''
complexity: 100
_liquidil_actual: 'Liquid error (line 1): undefined method ''[]'' for true'
