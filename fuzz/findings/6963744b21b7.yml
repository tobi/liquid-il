# Differential fuzzer finding -- see .goals/02-differential-fuzzer.md
---
name: fuzz_6963744b21b7
_metadata:
  doc: fuzz/README or goal 02 doc (.goals/02-differential-fuzzer.md)
  hint: 'rule=output_mismatch detail={index: 0, ref_ctx: "1.5Liquid er", lil_ctx:
    "Liquid error"}'
  signature: 'output_mismatch::[{type: :for, var: "id", else_body: [{type: :if, branches:
    [{cond: {type: :binop, op: "<=", left: {type: :chain, base: "id"}, right: {type:
    :chain, base: "id", accessors: [{dot: "id"}]}}}]}]}]'
  seed: 924303885
  subprocess_confirmed: true
template: "{% for value in nil %}{% else %}{% if data <= c.first %}{% endif %}{% endfor
  %}"
environment:
  data: 1.5
  c:
    product_name: false
expected: ''
complexity: 100
_liquidil_actual: 'Liquid error (line 1): comparison of Float with String failed'
