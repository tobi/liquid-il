# Differential fuzzer finding -- see .goals/02-differential-fuzzer.md
---
name: fuzz_536e6c3c5442
_metadata:
  doc: fuzz/README or goal 02 doc (.goals/02-differential-fuzzer.md)
  hint: 'rule=output_mismatch detail={index: 0, ref_ctx: "stray { brac", lil_ctx:
    "Liquid error"}'
  signature: 'output_mismatch::[{type: :for, var: "id", coll: {type: :range}, body:
    [{type: :if, branches: [{cond: {type: :binop, op: ">", left: {type: :chain, accessors:
    [{bracket: {type: :chain, base: "id", accessors: [{dot: "id"}]}}]}}}]}]}]'
  seed: 1876526781
  subprocess_confirmed: true
template: "{% for row in (nil..nil) %}{% if [row.url] > nil %}{% endif %}{% endfor
  %}"
environment: {}
expected: ''
complexity: 100
_liquidil_actual: 'Liquid error (line 1): no implicit conversion of String into Integer'
