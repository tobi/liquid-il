# Differential fuzzer finding -- see .goals/02-differential-fuzzer.md
---
name: fuzz_f614d955c787
_metadata:
  doc: fuzz/README or goal 02 doc (.goals/02-differential-fuzzer.md)
  hint: 'rule=output_mismatch detail={index: 16, ref_ctx: "y % percent\nstray { brac",
    lil_ctx: "y % percent\nLiquid error"}'
  signature: 'output_mismatch::[{type: :if, else_body: [{type: :unless, branches:
    [{body: [{type: :output, expr: {type: :filter, name: "truncatewords", args: [{}]}}]}]}]}]'
  seed: 1472973266
  subprocess_confirmed: true
template: "{% if false %}{% else %}{% unless nil %}{{ nil | truncatewords: nil }}{%
  endunless %}{% endif %}"
environment: {}
expected: ''
complexity: 100
_liquidil_actual: 'Liquid error (line 1): invalid integer'
