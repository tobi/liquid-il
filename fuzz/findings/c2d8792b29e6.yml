# Differential fuzzer finding -- see .goals/02-differential-fuzzer.md
---
name: fuzz_c2d8792b29e6
_metadata:
  doc: fuzz/README or goal 02 doc (.goals/02-differential-fuzzer.md)
  hint: 'rule=output_mismatch detail={index: 0, ref_ctx: "Hello, World", lil_ctx:
    "Liquid error"}'
  signature: 'output_mismatch::[{type: :unless, branches: [{body: [{type: :for, var:
    "id", coll: {type: :chain, base: "id"}, body: [{type: :unless, branches: [{cond:
    {type: :binop, op: "contains", right: {type: :chain, base: "id", accessors: [{dot:
    "id"}]}}}]}]}]}]}]'
  seed: 970669407
  subprocess_confirmed: true
template: "{% unless nil %}{% for node in user %}{% unless nil contains node.id %}{%
  endunless %}{% endfor %}{% endunless %}"
environment:
  user:
  - -226
expected: ''
complexity: 100
_liquidil_actual: 'Liquid error (line 1): no implicit conversion of String into Integer'
