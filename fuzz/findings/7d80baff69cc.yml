# Differential fuzzer finding -- see .goals/02-differential-fuzzer.md
---
name: fuzz_7d80baff69cc
_metadata:
  doc: fuzz/README or goal 02 doc (.goals/02-differential-fuzzer.md)
  hint: 'rule=output_mismatch detail={index: 0, ref_ctx: "-1stray % pe", lil_ctx:
    "Liquid error"}'
  signature: 'output_mismatch::[{type: :for, var: "id", coll: {type: :chain, base:
    "id"}, body: [{type: :for, var: "id", coll: {type: :chain, base: "id", accessors:
    [{dot: "id"}]}}]}]'
  seed: 1350154633
  subprocess_confirmed: true
template: "{% for item in values %}{% for row in item.id %}{% endfor %}{% endfor %}"
environment:
  values:
    key_67: {}
expected: ''
complexity: 100
_liquidil_actual: 'Liquid error (line 1): no implicit conversion of String into Integer'
