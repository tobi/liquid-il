# Differential fuzzer finding -- see .goals/02-differential-fuzzer.md
---
name: fuzz_75d3ff8834d0
_metadata:
  doc: fuzz/README or goal 02 doc (.goals/02-differential-fuzzer.md)
  hint: 'rule=output_mismatch detail={index: 0, ref_ctx: "1.5Liquid er", lil_ctx:
    "Liquid error"}'
  signature: 'output_mismatch::[{type: :for, var: "id", coll: {type: :range}, body:
    [{type: :if, branches: [{cond: {type: :logical, op: "or", right: {type: :binop,
    op: "contains", right: {type: :chain, base: "id", accessors: [{dot: "id"}]}}}}]}]}]'
  seed: 333198010
  subprocess_confirmed: true
template: "{% for node in (nil..nil) %}{% if nil or nil contains node.id %}{% endif
  %}{% endfor %}"
environment: {}
expected: ''
complexity: 100
_liquidil_actual: 'Liquid error (line 1): no implicit conversion of String into Integer'
