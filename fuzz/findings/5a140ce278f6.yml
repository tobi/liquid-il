# Differential fuzzer finding -- see .goals/02-differential-fuzzer.md
---
name: fuzz_5a140ce278f6
_metadata:
  doc: fuzz/README or goal 02 doc (.goals/02-differential-fuzzer.md)
  hint: "rule=output_mismatch detail={index: 0, ref_ctx: \"10.0emoji \U0001F600\\n\",
    lil_ctx: \"Liquid error\"}"
  signature: 'output_mismatch::[{type: :for, var: "id", coll: {type: :range}, body:
    [{type: :if, branches: [{cond: {type: :chain, base: "id"}, body: [{type: :for,
    var: "id", coll: {type: :range}, body: [{type: :output, expr: {type: :chain, base:
    "id", accessors: [{dot: "id"}]}}]}]}]}]}]'
  seed: 622838087
  subprocess_confirmed: true
template: "{% for node in (nil..nil) %}{% if count %}{% for elem in (nil..nil) %}{{
  elem.name }}{% endfor %}{% endif %}{% endfor %}"
environment:
  count: 0.0
expected: ''
complexity: 100
_liquidil_actual: 'Liquid error (line 1): no implicit conversion of String into Integer'
