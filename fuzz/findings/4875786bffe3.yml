# Differential fuzzer finding -- see .goals/02-differential-fuzzer.md
---
name: fuzz_4875786bffe3
_metadata:
  doc: fuzz/README or goal 02 doc (.goals/02-differential-fuzzer.md)
  hint: 'rule=output_mismatch detail={index: 18, ref_ctx: "arkup</div>\n", lil_ctx:
    "arkup</div>\ntrue"}'
  signature: 'output_mismatch::[{type: :assign, name: "id", expr: {type: :chain, base:
    "id"}}, {type: :if, else_body: [{type: :cycle, values: [{type: :chain, base: "id",
    accessors: [{dot: "id"}]}]}]}]'
  seed: 724950052
  subprocess_confirmed: true
template: "{% assign item = nums %}{% if false %}{% else %}{% cycle item.url %}{%
  endif %}"
environment:
  nums: true
expected: ''
complexity: 100
_liquidil_actual: 'true'
