# Differential fuzzer finding -- see .goals/02-differential-fuzzer.md
---
name: fuzz_bbad8f80d2f3
_metadata:
  doc: fuzz/README or goal 02 doc (.goals/02-differential-fuzzer.md)
  hint: 'rule=output_mismatch detail={index: 0, ref_ctx: "stray % perc", lil_ctx:
    "Liquid error"}'
  signature: 'output_mismatch::[{type: :for, var: "id", coll: {type: :range}, body:
    [{type: :output, expr: {type: :filter, target: {type: :chain, base: "id"}, name:
    "sort"}}]}]'
  seed: 247102805
  subprocess_confirmed: true
template: "{% for entry in (nil..nil) %}{{ forloop | sort }}{% endfor %}"
environment: {}
expected: Liquid::ForloopDrop
complexity: 100
_liquidil_actual: ForloopDrop
