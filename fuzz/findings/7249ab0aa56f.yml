# Differential fuzzer finding -- see .goals/02-differential-fuzzer.md
---
name: fuzz_7249ab0aa56f
_metadata:
  doc: fuzz/README or goal 02 doc (.goals/02-differential-fuzzer.md)
  hint: 'rule=output_mismatch detail={index: 0, ref_ctx: "[true]stray ", lil_ctx:
    "Liquid error"}'
  signature: 'output_mismatch::[{type: :for, var: "id", coll: {type: :chain, base:
    "id"}, body: [{type: :if, branches: [{cond: {type: :binop, op: "!=", left: {type:
    :chain, base: "id", accessors: [{dot: "id"}]}}}]}]}]'
  seed: 1368358709
  subprocess_confirmed: true
template: "{% for node in item %}{% if node.url != nil %}{% endif %}{% endfor %}"
environment:
  item:
  - false
expected: ''
complexity: 100
_liquidil_actual: 'Liquid error (line 1): undefined method ''[]'' for false'
