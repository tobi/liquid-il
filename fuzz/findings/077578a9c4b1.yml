# Differential fuzzer finding -- see .goals/02-differential-fuzzer.md
---
name: fuzz_077578a9c4b1
_metadata:
  doc: fuzz/README or goal 02 doc (.goals/02-differential-fuzzer.md)
  hint: 'rule=output_mismatch detail={index: 12, ref_ctx: "Liquid error: invalid in",
    lil_ctx: "Liquid error (line 1): n"}'
  signature: 'output_mismatch::[{type: :for, var: "id", coll: {type: :range}, body:
    [{type: :if, branches: [{cond: {type: :binop, op: "!=", right: {type: :chain,
    base: "id", accessors: [{dot: "id"}]}}}]}]}]'
  seed: 594583495
  subprocess_confirmed: true
template: "{% for it in (nil..nil) %}{% if nil != it.id %}{% endif %}{% endfor %}"
environment: {}
expected: ''
complexity: 100
_liquidil_actual: 'Liquid error (line 1): no implicit conversion of String into Integer'
