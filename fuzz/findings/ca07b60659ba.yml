# Differential fuzzer finding -- see .goals/02-differential-fuzzer.md
---
name: fuzz_ca07b60659ba
_metadata:
  doc: fuzz/README or goal 02 doc (.goals/02-differential-fuzzer.md)
  hint: 'rule=output_mismatch detail={index: 0, ref_ctx: "0Hello, Worl", lil_ctx:
    "Liquid error"}'
  signature: 'output_mismatch::[{type: :for, var: "id", coll: {type: :range}, body:
    [{type: :case, whens: [{body: [{type: :if, branches: [{cond: {type: :logical,
    op: "and", left: {type: :binop, op: "==", right: {type: :chain, accessors: [{bracket:
    {type: :chain, base: "id", accessors: [{dot: "id"}]}}]}}}}]}]}]}]}]'
  seed: 201825172
  subprocess_confirmed: true
template: "{% for item in (nil..nil) %}{% case nil %}{% when nil %}{% if nil == [item.value]
  and nil %}{% endif %}{% endcase %}{% endfor %}"
environment: {}
expected: ''
complexity: 100
_liquidil_actual: 'Liquid error (line 1): no implicit conversion of String into Integer'
