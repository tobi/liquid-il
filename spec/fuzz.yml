# Staging file for curated fuzz-derived behavior specs, pending donation to
# Shopify/liquid-spec.
#
# CURRENTLY EMPTY: the first curation round (26 rules distilled from ~400
# minimized differential-fuzzer findings, see fuzz/triage/) was donated to
# liquid-spec in commits b7a41af (blank_body_error_handling.yml,
# case_when_matching.yml, case_basic hint fix, QUIRKS entries) and 3ef0156
# (comparison_semantics.yml, filter_argument_semantics.yml,
# scalar_and_iteration_semantics.yml).
#
# Workflow for future rounds:
#   1. rake fuzz            — chronicle new findings under fuzz/findings/
#   2. rake fuzz:export     — raw dump to fuzz/triage/raw_export.yml
#   3. editorial triage     — rules not findings; accidents go to
#                             fuzz/reference_bug_candidates.md, not specs
#   4. stage curated specs HERE, validated against reference liquid with
#      line_numbers: true and per-spec error_mode
#   5. donate to liquid-spec, then empty this file again
#
# This file is never regenerated mechanically and is not auto-discovered by
# the test suite.
---
specs: []
