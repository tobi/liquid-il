# Divergences found by LiquidIL's differential fuzzer (fuzz/ in
# github.com/tobi/liquid-il) between compiled-Liquid implementations and
# reference liquid. Every expected: value was re-validated against the
# reference gem at export time. Intended for donation to liquid-spec.
#
# Regenerate: bundle exec rake fuzz:export
---
- name: fuzz_abs_70c74d757b7c
  template: "{{ nil | truncatewords: nil | abs }}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 708480582) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_abs_86aa44ceb246
  template: "{% for row in nil %}{% else %}{% assign c = nil | abs %}{% cycle c.url
    %}{% endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1025366034) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "0".
- name: fuzz_abs_a1ce738f4972
  template: "{% echo record | lstrip | abs %}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1141720933) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "\u0000".
- name: fuzz_abs_d7f5fa0229c7
  template: "{{ item | upcase | abs }}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1928142476) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "\u0000".
- name: fuzz_abs_dddcc32cbc06
  template: "{% if nil != info %}{% capture list %}{{ info | upcase | abs }}{% endcapture
    %}{% endif %}"
  environment:
    info: 0.0
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1742854968) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of BigDecimal into String".
- name: fuzz_abs_fa9179596839
  template: "{{ nil | truncate: nil | abs }}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1572320778) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_abs_ffceea09151c
  template: |-
    {% liquid
    echo value | downcase | abs
    %}
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 322577280) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "\u0000".
- name: fuzz_append_2c93a9bc5d55
  template: "{% if false %}{% else %}{% assign config = nil | append %}{% endif %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 628217479) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): wrong number of arguments (given 1, expected 2)".
- name: fuzz_at_least_595133b4948f
  template: "{% assign value = nil | at_least %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 287211211) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): wrong number of arguments (given 1, expected 2)".
- name: fuzz_capitalize_062257c44131
  template: "{% for value in a %}{{ forloop | capitalize }}{% endfor %}"
  environment:
    a: _S_marker_483
  expected: Liquid::forloopdrop
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 774694435) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Forloopdrop".
- name: fuzz_capitalize_7796b600e0be
  template: "{{ nil | truncate: nil | floor | capitalize }}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1773384369) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_capitalize_87bebc42ea39
  template: "{% for node in (nil..nil) %}{{ nil | json | capitalize }}{% endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1281206877) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Null".
- name: fuzz_capitalize_b84d003f725d
  template: "{{ nil | truncatewords: nil | capitalize }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 286786719) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_case_051e8868cab8
  template: "{% case nil %}{% when nil, nil %}{% cycle 0.0 %}{% endcase %}"
  expected: 0.00.0
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 41195287) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "0.0".
- name: fuzz_case_1004794540c6
  template: '{% case nil %}{% when nil %}{% cycle ["_S_marker_"] %}{% endcase %}'
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 916485656) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "_S_marker_".
- name: fuzz_case_1210b745fbdb
  template: "{% case nil %}{% when nil, nil %}{% cycle 1 %}{% endcase %}"
  expected: '11'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 292021528) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "1".
- name: fuzz_case_124cfed626ce
  template: '{% case nil %}{% when [[["{% not_a_tag %}"]]] %}{% endcase %}'
  expected: "\"]]] %}"
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 2067568675) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_case_203a4098b577
  template: "{% case nil %}{% else %}{% if y > item.size %}{% tablerow item in item[nil]
    %}{% endtablerow %}{% endif %}{% endcase %}"
  environment:
    item: []
    "y": 4611686018427387904
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1774344090) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "<tr class=\"row1\">\n</tr>\n".
- name: fuzz_case_281648ea8f2e
  template: "{% case nil %}{% when nil, nil %}{{ z }}{% endcase %}"
  environment:
    z: {}
  expected: "{}{}"
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1625749386) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "{}".
- name: fuzz_case_2c3a6d79ca60
  template: "{% case nil %}{% when nil, nil %}{{ true }}{% endcase %}"
  expected: truetrue
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 953330229) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "true".
- name: fuzz_case_2e1ab2d41c18
  template: '{% case nil %}{% else %}{% if items > "_S_marker_" %}{% endif %}{% endcase
    %}'
  environment:
    items: -0.5
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 887368864) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): comparison of Float with String failed".
- name: fuzz_case_31d0111f8a7e
  template: "{% case nil %}{% when nil %}{% cycle [z] %}{% endcase %}"
  environment:
    z: true
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1367081837) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "true".
- name: fuzz_case_39eb8ef523be
  template: "{% case nil %}{% when nil %}{% cycle info %}{% endcase %}"
  environment:
    info: []
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 135124762) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "[]".
- name: fuzz_case_3ab21881ed93
  template: "{% case nil %}{% when nil, nil %}{% tablerow item in value %}{% endtablerow
    %}{% endcase %}"
  environment:
    value: 1.5
  expected: |
    <tr class="row1">
    </tr>
    <tr class="row1">
    </tr>
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1405640085) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "<tr class=\"row1\">\n</tr>\n".
- name: fuzz_case_4a46fdc90ace
  template: |-
    {% case nil %}{% else %}{% raw %}50% off
    {% endraw %}{{- nil -}}{% endcase %}
  expected: '50% off

    '
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 101308294) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "50% off".
- name: fuzz_case_4c946dfb8a07
  template: "{% case nil %}{% else %}{% unless nil %}{% cycle data %}{% endunless
    %}{% endcase %}"
  environment:
    data: []
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 412233517) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "[]".
- name: fuzz_case_50c2836c35b0
  template: "{% case nil %}{% when nil, nil %}{% cycle x %}{% endcase %}"
  environment:
    x: {}
  expected: "{}{}"
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 2061115068) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "{}".
- name: fuzz_case_5a55a55d463a
  template: |-
    {% case nil %}{% when nil, nil %}{% liquid
    echo a
    %}{% endcase %}
  environment:
    a:
    - {}
  expected: "{}{}"
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 787553212) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "{}".
- name: fuzz_case_60c7f9bfa256
  template: '{% case nil %}{% when nil, nil %}  {% when nil %}{% unless nil %}{% cycle
    "" %}{% endunless %}{% endcase %}'
  expected: "    "
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 776044953) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "  ".
- name: fuzz_case_6ba504c7c212
  template: |-
    {% case nil %}{% when nil, nil %}price: $9.99
    {% endcase %}
  expected: |
    price: $9.99
    price: $9.99
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1626737164) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "price: $9.99\n".
- name: fuzz_case_6c9108d2c39c
  template: |-
    {% case nil %}{% when nil, nil %}{% for row in (nil..nil) %}50% off
    {% endfor %}{% endcase %}
  expected: |
    50% off
    50% off
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1955368333) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "50% off\n".
- name: fuzz_case_74d06495e808
  template: "{% case nil %}{% when nil, nil %}{% increment  %}{% endcase %}"
  expected: '01'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1436228005) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "0".
- name: fuzz_case_7a7c8eae75a2
  template: "{% case nil %}{% when nil, nil %}{% raw %}emoji \U0001F600\n{% endraw
    %}{% endcase %}"
  expected: "emoji \U0001F600\nemoji \U0001F600\n"
  complexity: 800
  hint: "Recorded by LiquidIL's differential fuzzer (seed 1491966586) and\nconfirmed
    against reference liquid in a clean subprocess: the expected\noutput is exactly
    what the reference gem renders in strict mode.\nA compiling implementation diverged
    here at recording time,\nproducing: \"emoji \U0001F600\\n\".\n"
- name: fuzz_case_7c26e3b60d93
  template: "{% case nil %}{% when nil, nil %}{% for node in (nil..nil) %}{{ config
    }}{% endfor %}{% endcase %}"
  environment:
    config: {}
  expected: "{}{}"
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 813360780) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "{}".
- name: fuzz_case_7eafce296fe0
  template: |-
    {% case nil %}{% when nil, nil %}{% liquid
    echo -4611686018427387904
    %}{% endcase %}
  expected: "-4611686018427387904-4611686018427387904"
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1020295461) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "-4611686018427387904".
- name: fuzz_case_8fd9e56fac54
  template: "{% case nil %}{% when nil, nil %}{{ 0.0 }}{% endcase %}"
  expected: 0.00.0
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1246005754) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "0.0".
- name: fuzz_case_a1a5d9333931
  template: "{% case nil %}{% when nil %}{% cycle true %}{% endcase %}"
  expected: 'true'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1244185055) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_case_a241c23b81de
  template: "{% case nil %}{% else %}{% cycle values[nil] %}{% endcase %}"
  environment:
    values: PRICE_START_99_END
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1969237406) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "PRICE_START_99_END".
- name: fuzz_case_a88cad685914
  template: "{% case nil %}{% when nil %}{% for item in (nil..nil) %}{% unless nil
    or item.name %}{% endunless %}{% endfor %}{% endcase %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 427788210) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_case_b59c8e9f0727
  template: '{% case nil %}{% when "{% not_a_tag %}" %}{% endcase %}'
  expected: "\" %}"
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1007041045) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_case_be9d15da2056
  template: "{% case nil %}{% else %}{% cycle true %}{% endcase %}"
  expected: 'true'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 976452688) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_case_cbd8b5904df1
  template: "{% case nil %}{% when nil %}{% for elem in (nil..nil) %}{% tablerow entry
    in c.first %}{% endtablerow %}{% endfor %}{% endcase %}"
  environment:
    c:
      name: []
  expected: |
    <tr class="row1">
    <td class="col1"></td><td class="col2"></td></tr>
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1092871535) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "<tr class=\"row1\">\n<td class=\"col1\"></td></tr>\n".
- name: fuzz_case_e3f19b025911
  template: "{% case nil %}{% when nil %}{% cycle false %}{% endcase %}"
  expected: 'false'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 758557153) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_case_e705fe4f591c
  template: |-
    {% case nil %}{% when nil, nil %}  {% when nil %}{% for entry in nil %}a { brace } c
    {% endfor %}{% endcase %}
  expected: "    "
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 302723867) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "  ".
- name: fuzz_case_ed999f3cf702
  template: "{% case nil %}{% when nil %}{% unless nil %}{% tablerow value in total[nil]
    %}{% endtablerow %}{% endunless %}{% endcase %}"
  environment:
    total: true
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 288148242) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "<tr class=\"row1\">\n</tr>\n".
- name: fuzz_case_f399ec4e267e
  template: '{% case nil %}{% when "Hello, World!" %}{% decrement  %}{% endcase %}'
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1943480533) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "-1".
- name: fuzz_case_f5c75c757c3d
  template: "{% case nil %}{% when nums[0] %}tab\there\n{% endcase %}"
  environment:
    nums:
      '0': true
  expected: "tab\there\n"
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 815755504) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_case_fb9883ade7d9
  template: "{% case nil %}{% when nil %}{% tablerow entry in nums[nil] %}{% endtablerow
    %}{% endcase %}"
  environment:
    nums: 0
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 981132994) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "<tr class=\"row1\">\n</tr>\n".
- name: fuzz_case_fe7e6792d599
  template: '{% case nil %}{% when [[[[["{% not_a_tag %}88"]]]]] %}{% endcase %}'
  expected: 88"]]]]] %}
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 423991958) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_ceil_03208c8bbae7
  template: "{% echo total | capitalize | ceil %}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1738981539) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "\u0000".
- name: fuzz_ceil_3701b0622427
  template: "{{ names | lstrip | ceil }}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 423797998) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "\u0000".
- name: fuzz_ceil_94dcdb5f4527
  template: "{{ b | downcase | ceil }}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1150868384) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "\u0000".
- name: fuzz_ceil_9c15f87392a5
  template: "{% unless nil %}{{ nil | truncate: nil | ceil }}{% endunless %}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 2139605436) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_ceil_b7aab891f6e8
  template: "{% echo total | lstrip | ceil %}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1120424052) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "\u0000".
- name: fuzz_ceil_bddabce4c050
  template: "{{ product | rstrip | lstrip | ceil }}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 509347543) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "\u0000".
- name: fuzz_ceil_e91df6af3cf5
  template: "{% echo nums | upcase | ceil %}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 486185420) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "\u0000".
- name: fuzz_ceil_eb59f2b8b79d
  template: "{% for elem in nil %}{% else %}{{ c | ceil | downcase | ceil }}{% endfor
    %}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 47403331) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "\u0000".
- name: fuzz_compact_0480cebc2108
  template: "{% if nil == nil %}{{ nil | truncate: nil | uniq | compact }}{% endif
    %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1031484936) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_compact_26addf605ee5
  template: "{{ nil | truncatewords: nil | last | compact }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 2144939031) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_compact_b61663d74197
  template: |-
    {% liquid
    assign b = nil | json | compact
    %}{% echo b %}
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1265505057) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "null".
- name: fuzz_compact_c6c9c39af189
  template: "{{ nil | truncatewords: nil | compact }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 2081679699) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_compact_d75200fbf933
  template: "{% echo nil | json | compact %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1387967485) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "null".
- name: fuzz_compact_e855db4c39fe
  template: "{{ count | ceil | compact }}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1231144337) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "[0]".
- name: fuzz_compact_f344571b3156
  template: "{{ x | floor | downcase | compact }}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1351695139) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "[\"0\"]".
- name: fuzz_compact_f706cbf22560
  template: "{% if nil or true or nil %}{{ nil | truncatewords: nil | compact }}{%
    endif %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 484275990) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_cycle_08020e920409
  template: "{% cycle true %}"
  expected: 'true'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 247911277) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_cycle_0b09309c4048
  template: '{% cycle ["日本語こんにちは"] %}'
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1315831483) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "日本語こんにちは".
- name: fuzz_cycle_3275f173af7e
  template: "{% cycle false %}"
  expected: 'false'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1385882320) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_cycle_471f07750dc1
  template: "{% cycle [3.14159] %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 888801313) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "3.14159".
- name: fuzz_cycle_61dbaa5ce3e3
  template: "{% cycle [data] %}"
  environment:
    data: {}
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1503114360) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "{}".
- name: fuzz_cycle_662d67384089
  template: "{% cycle items %}"
  environment:
    items: []
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1967759102) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "[]".
- name: fuzz_cycle_cb51acd5e73d
  template: '{% cycle "a,b,c,d813" %}'
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 91144070) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "a,b,c,d813".
- name: fuzz_cycle_d23eb8ea4c11
  template: "{% cycle product.id %}"
  environment:
    product: {}
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 751616352) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "{}".
- name: fuzz_cycle_d7b9a4f635c4
  template: "{% cycle item[nil] %}"
  environment:
    item: false
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 600415114) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "false".
- name: fuzz_cycle_dbb28efa25a2
  template: "{% cycle [0] %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 489516471) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "0".
- name: fuzz_default_0693c71a21c7
  template: "{{ nil | truncate: nil | default }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 115054333) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_default_0bbfe68e1032
  template: "{{ nil | truncatewords: nil | default }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 2116500756) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_default_30f41e71f27d
  template: "{{ nil | json | default }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1933992308) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "null".
- name: fuzz_default_71236a0c30d4
  template: "{% for node in (nil..nil) %}{% echo nil | json | strip_newlines | default
    %}{% endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1599052310) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "null".
- name: fuzz_default_9b3c206aee37
  template: "{% assign data = nil | json %}{{ nil | default: data }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 432026307) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "null".
- name: fuzz_default_a8092861c465
  template: |-
    {% liquid
    echo nil | truncate: nil | default
    %}
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1511158756) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_default_d24b7be58c93
  template: "{% echo nil | truncatewords: nil | default %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1987746127) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_divided_by_f3646009f45c
  template: |-
    {% liquid
    assign item = nil | prepend | divided_by
    %}
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1675341126) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): wrong number of arguments (given 1, expected 2)".
- name: fuzz_downcase_56d409414c43
  template: "{% for it in nil %}{% else %}{{ nil | truncate: nil | downcase }}{% endfor
    %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 2042381343) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_downcase_977267185d2b
  template: "{% unless nil %}{{ nil | split | downcase }}{% endunless %}"
  render_errors: true
  expected: 'Liquid error: wrong number of arguments (given 1, expected 2)'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 780421738) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "[]".
- name: fuzz_escape_4b8bb834b0a5
  template: "{% for elem in list %}{% echo [elem.url] | escape %}{% endfor %}"
  environment:
    list:
    - 0
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1788446791) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_escape_8b1cf9865d05
  template: "{% assign user = nil | truncatewords: nil %}{% echo user | escape %}"
  environment:
    user: []
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1399490613) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "[]".
- name: fuzz_escape_a45d388ffa55
  template: "{{ nil | truncatewords: nil | escape }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1762708982) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_escape_c717bb090c0a
  template: "{{ nil | truncate | compact | escape }}"
  expected: "[]"
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1892136505) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "[&quot;&quot;]".
- name: fuzz_escape_e6d018c615cf
  template: "{{ nil | json | escape }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 27312723) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "null".
- name: fuzz_escape_once_5f20fb0f33b0
  template: "{% if false %}{% else %}{% unless nil %}{% echo nil | truncate: nil |
    escape_once %}{% endunless %}{% endif %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 601743313) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_escape_once_9c9574b74911
  template: "{{ nil | truncate: nil | truncate | escape_once }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 2071961199) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_escape_once_c2fbb32e1acf
  template: "{% unless nil %}{{ nil | truncate: nil | escape_once }}{% endunless %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1650553442) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_first_49415cbfbc60
  template: "{{ nil | truncatewords: nil | first }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 708364642) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_first_5b81dde6ded5
  template: "{{ [nil] | rstrip | first }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1622581856) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of nil into String".
- name: fuzz_first_742db4525e35
  template: "{{ nil | truncate: nil | first }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 735924262) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_first_79703fd21f7e
  template: "{% echo nil | json | first %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1143418950) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "n".
- name: fuzz_first_9114c8491fad
  template: "{{ nil | json | first }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1294467374) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "n".
- name: fuzz_floor_24f048b06aac
  template: "{% case nil %}{% when nil, nil %}{{ nil | floor }}{% endcase %}"
  expected: '00'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 2038531714) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "0".
- name: fuzz_floor_3afb74ce23e7
  template: "{% if nil != false %}{{ entry | lstrip | floor }}{% endif %}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1131074524) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "\u0000".
- name: fuzz_floor_65bc82499945
  template: "{% if list %}{{ nil | truncatewords: nil | floor }}{% endif %}"
  environment:
    list: []
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1232957763) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_floor_6fbd4d7d25bb
  template: "{% case nil %}{% when nil %}{% if 4611686018427387904 %}{{ data | strip
    | floor }}{% endif %}{% endcase %}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 321865617) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "\u0000".
- name: fuzz_floor_c867bb036c53
  template: |-
    {% liquid
    echo nil | truncate: nil | floor
    %}
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 13780429) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_floor_cab9dc3b2b3f
  template: "{{ nil | truncate: nil | floor }}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 402556578) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_floor_d7585f64d511
  template: "{% if false %}{% else %}{% echo names | downcase | floor %}{% endif %}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 553497657) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "\u0000".
- name: fuzz_floor_da9ce3db5f7d
  template: "{{ y | downcase | floor }}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 151831905) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "\u0000".
- name: fuzz_floor_ebfe622be765
  template: |-
    {% liquid
    echo y | strip | floor
    %}
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1014836987) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "\u0000".
- name: fuzz_for_02195f9cad0b
  template: "{% for entry in (nil..nil) %}{% case nil %}{% when entry.title %}{% endcase
    %}{% endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1423700843) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_for_077578a9c4b1
  template: "{% for it in (nil..nil) %}{% if nil != it.id %}{% endif %}{% endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 594583495) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_for_07b4fd8778bb
  template: "{% for item in (nil..nil) %}{% cycle false %}{% endfor %}"
  expected: 'false'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1905266956) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_for_0ac843050214
  template: "{% for node in (nil..nil) %}{% if nil contains [node.title] and nil %}{%
    endif %}{% endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 2019034463) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_for_0f4ec637ab7f
  template: |-
    {% for row in nil %}{% else %}{% case nil %}{% when nil, nil %}stray % percent
    {% endcase %}{% endfor %}
  expected: |
    stray % percent
    stray % percent
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 553868732) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "stray % percent\n".
- name: fuzz_for_12036b87a5dc
  template: |-
    {% for node in (nil..nil) %}{% for it in forloop %}a { brace } c
    {% endfor %}{% endfor %}
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1120448721) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "a { brace } c\n".
- name: fuzz_for_164f3cdd5e24
  template: "{% for entry in (nil..nil) %}{% if forloop %}{% cycle forloop %}{% endif
    %}{% endfor %}"
  expected: Liquid::ForloopDrop
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1808583138) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "ForloopDrop".
- name: fuzz_for_25b257a4aee5
  template: '{% for it in (nil..nil) %}{% for node in (nil..nil) %}{% if forloop >
    "  padded  " and nil %}{% endif %}{% endfor %}{% endfor %}'
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 661041110) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): comparison of LiquidIL::ForloopDrop with String failed".
- name: fuzz_for_29cf71ade2e1
  template: "{% for item in (nil..nil) %}{% if item.price contains nil %}{% endif
    %}{% endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 535627418) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_for_310e2c6761b1
  template: "{% for it in (nil..nil) %}{% if nil or it.title > nil %}{% endif %}{%
    endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1483417885) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_for_3218f6e68c99
  template: "{% for elem in (nil..nil) %}{% cycle [forloop] %}{% endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1701729969) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "ForloopDrop".
- name: fuzz_for_35b3e717fb71
  template: "{% for row in (nil..nil) %}{% for value in row.name %}{% endfor %}{%
    endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 143561312) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_for_3749721c8764
  template: "{% for it in c %}{% case it.url %}{% endcase %}{% endfor %}"
  environment:
    c:
      id: 3.14159
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1781782234) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_for_386f38c7d0a2
  template: "{% for row in (nil..nil) %}{% tablerow row in values %}{% cycle self
    %}{% endtablerow %}{% endfor %}"
  environment:
    values:
      name: PRICE_START_99_END
  expected: |
    <tr class="row1">
    <td class="col1">Liquid::SelfDrop</td></tr>
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 2004026303) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "<tr class=\"row1\">\n<td class=\"col1\"></td></tr>\n".
- name: fuzz_for_3c9c871dbf06
  template: "{% for entry in (nil..nil) %}{% tablerow elem in [nil] %}{% endtablerow
    %}{% endfor %}"
  environment:
    items: hello854
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1192811016) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "<tr class=\"row1\">\n<td class=\"col1\"></td></tr>\n".
- name: fuzz_for_3dca3ede34d9
  template: "{% for entry in (nil..nil) %}{% unless nil %}{% tablerow entry in entry[nil]
    %}{% endtablerow %}{% endunless %}{% endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 642035961) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "<tr class=\"row1\">\n</tr>\n".
- name: fuzz_for_3f7dbda1a7a5
  template: "{% for it in nil %}{% else %}{% cycle config %}{% endfor %}"
  environment:
    config: []
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 652303829) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "[]".
- name: fuzz_for_5166c65e9871
  template: "{% for node in (nil..nil) %}{% case nil %}{% when nil %}{% cycle forloop
    %}{% endcase %}{% endfor %}"
  expected: Liquid::ForloopDrop
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1216500411) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "ForloopDrop".
- name: fuzz_for_5225b5ca9e71
  template: "{% for row in (nil..nil) %}{% tablerow item in row[nil] %}{% endtablerow
    %}{% endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1591177300) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "<tr class=\"row1\">\n</tr>\n".
- name: fuzz_for_531c1a9dfcb5
  template: "{% for row in c %}{% if forloop %}{% cycle false %}{% endif %}{% endfor
    %}"
  environment:
    c:
      key_24: []
  expected: 'false'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 2119625034) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_for_536e6c3c5442
  template: "{% for row in (nil..nil) %}{% if [row.url] > nil %}{% endif %}{% endfor
    %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1876526781) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_for_53dab3be7b46
  template: |-
    {% for elem in (nil..nil) %}{% liquid
    echo forloop
    %}{% endfor %}
  expected: Liquid::ForloopDrop
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1824991356) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "ForloopDrop".
- name: fuzz_for_5702c8dd3fdd
  template: "{% for entry in (nil..nil) %}{% case nil %}{% when nil %}{% case entry.count
    %}{% endcase %}{% endcase %}{% endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 687338758) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_for_5a140ce278f6
  template: "{% for node in (nil..nil) %}{% if count %}{% for elem in (nil..nil) %}{{
    elem.name }}{% endfor %}{% endif %}{% endfor %}"
  environment:
    count: 0.0
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 622838087) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_for_68373d6fee17
  template: "{% for entry in (nil..nil) %}{{ forloop }}{% endfor %}"
  expected: Liquid::ForloopDrop
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 424825298) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "ForloopDrop".
- name: fuzz_for_6963744b21b7
  template: "{% for value in nil %}{% else %}{% if data <= c.first %}{% endif %}{%
    endfor %}"
  environment:
    data: 1.5
    c:
      product_name: false
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 924303885) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): comparison of Float with String failed".
- name: fuzz_for_6e1a804d6424
  template: "{% for node in (nil..nil) %}{% case nil %}{% when .first %}{% increment
    \ %}{% endcase %}{% endfor %}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 64565017) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_for_7249ab0aa56f
  template: "{% for node in item %}{% if node.url != nil %}{% endif %}{% endfor %}"
  environment:
    item:
    - false
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1368358709) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): undefined method '[]' for false".
- name: fuzz_for_72ff59568daa
  template: "{% for entry in (nil..nil) %}{% unless entry.value %}{% endunless %}{%
    endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 66784982) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_for_748187a025b0
  template: "{% for entry in items %}{% if nil contains [entry.count] %}{% endif %}{%
    endfor %}"
  environment:
    items:
    - true
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 838087336) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): undefined method '[]' for true".
- name: fuzz_for_75d3ff8834d0
  template: "{% for node in (nil..nil) %}{% if nil or nil contains node.id %}{% endif
    %}{% endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 333198010) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_for_7a5411eee4a4
  template: |-
    {% for value in (nil..nil) %}{% for value in forloop %}{% raw %}Hello, World!
    {% endraw %}{% endfor %}{% endfor %}
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 277363065) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Hello, World!\n".
- name: fuzz_for_7d7af672b9b7
  template: "{% for value in (nil..nil) %}{% if -0.5 > data %}{% endif %}{% endfor
    %}"
  environment:
    data: café
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1827101700) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): comparison of Float with String failed".
- name: fuzz_for_7d80baff69cc
  template: "{% for item in values %}{% for row in item.id %}{% endfor %}{% endfor
    %}"
  environment:
    values:
      key_67: {}
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1350154633) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_for_7e6648c8306b
  template: "{% for it in flag %}{% cycle false %}{% endfor %}"
  environment:
    flag:
    - []
  expected: 'false'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1984727016) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_for_7ef3826d2a1b
  template: "{% for value in y %}{% cycle y.value %}{% endfor %}"
  environment:
    "y": 'false'
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1224254555) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "false".
- name: fuzz_for_89607e8c74a9
  template: "{% for value in (nil..nil) %}{{ value.url }}{% endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1181132308) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_for_8b38bcf32e62
  template: "{% for entry in (nil..nil) %}{% unless nil  [entry.value] %}{% endunless
    %}{% endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1263696658) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_for_9194e7fe4406
  template: "{% for node in values %}{% cycle [z] %}{% endfor %}"
  environment:
    values:
    - {}
    z: 3.14159
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 173319661) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "3.14159".
- name: fuzz_for_942fac0cc2b5
  template: "{% for value in (nil..nil) %}{% if true and nil  [value.url] contains
    nil %}{% endif %}{% endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1101270377) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_for_9c3f6e0cf8fd
  template: '{% for entry in (nil..nil) %}{% if "_S_marker_" and nil or entry.url
    %}{% endif %}{% endfor %}'
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 246582510) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_for_a21e319b2524
  template: "{% for row in (nil..nil) %}{% if row.price %}{% endif %}{% endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 541748717) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_for_a4e523f0d069
  template: "{% for node in (nil..nil) %}{% if [node.title] %}{% endif %}{% endfor
    %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 26837099) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_for_ab49aac0b845
  template: "{% for entry in (nil..nil) %}{% cycle entry.price %}{% endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1023044005) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "0".
- name: fuzz_for_b04df0774f85
  template: "{% for item in (nil..nil) %}{% if nil or nil or 0.0 %}{% if [item.url]
    and nil and nil %}{% endif %}{% endif %}{% endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 472462603) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_for_b1ab2c512221
  template: "{% for it in (nil..nil) %}{% cycle [-0.5] %}{% endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 235181388) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "-0.5".
- name: fuzz_for_b203b59091be
  template: |-
    {% for it in (nil..nil) %}{% case nil %}{% when nil, nil %}a { brace } c
    {% endcase %}{% endfor %}
  expected: |
    a { brace } c
    a { brace } c
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 96791649) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "a { brace } c\n".
- name: fuzz_for_b45b4bb6deaf
  template: "{% for row in nil %}{% else %}{% tablerow entry in entry[nil] %}{% endtablerow
    %}{% endfor %}"
  environment:
    entry: true
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1392075097) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "<tr class=\"row1\">\n</tr>\n".
- name: fuzz_for_b663bd3915aa
  template: "{% for elem in (nil..nil) %}{% for value in (nil..nil) %}{{ forloop }}{%
    endfor %}{% endfor %}"
  expected: Liquid::ForloopDrop
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1180293350) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "ForloopDrop".
- name: fuzz_for_b9d6e3a0d1ec
  template: "{% for row in (nil..nil) %}{% if nil contains row.count %}{% endif %}{%
    endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1175241680) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_for_bef8636ecc18
  template: "{% for it in values %}{{ forloop }}{% endfor %}"
  environment:
    values:
    - {}
  expected: Liquid::ForloopDrop
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 41309733) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "ForloopDrop".
- name: fuzz_for_c2ebe382de9a
  template: "{% for entry in nil %}{% else %}{% for value in (nil..nil) %}{% unless
    nil contains value.title %}{% endunless %}{% endfor %}{% endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1678468802) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_for_ca07b60659ba
  template: "{% for item in (nil..nil) %}{% case nil %}{% when nil %}{% if nil ==
    [item.value] and nil %}{% endif %}{% endcase %}{% endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 201825172) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_for_cb4674f9c5c9
  template: "{% for row in z %}{% cycle forloop %}{% endfor %}"
  environment:
    z:
      '0': {}
  expected: Liquid::ForloopDrop
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 156657355) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "ForloopDrop".
- name: fuzz_for_d39912c8aad1
  template: "{% for node in (nil..nil) %}{% unless nil contains node.count %}{% endunless
    %}{% endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1623432469) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_for_d5823c7b92f5
  template: "{% for value in (nil..nil) %}{% if nil > value.name %}{% endif %}{% endfor
    %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1749314265) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_for_d73bd32fd950
  template: "{% for node in (nil..nil) %}{% unless nil %}{% cycle values[nil] %}{%
    endunless %}{% endfor %}"
  environment:
    values: -0.5
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1168410374) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "-0.5".
- name: fuzz_for_dae6f584e107
  template: "{% for node in (nil..nil) %}{% cycle forloop %}{% endfor %}"
  expected: Liquid::ForloopDrop
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1338773453) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "ForloopDrop".
- name: fuzz_for_e852473159e2
  template: "{% for item in (nil..nil) %}{% unless nil %}{% cycle forloop %}{% endunless
    %}{% endfor %}"
  expected: Liquid::ForloopDrop
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 793284896) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "ForloopDrop".
- name: fuzz_for_ea2298afa334
  template: "{% for elem in (nil..nil) %}{% cycle true %}{% endfor %}"
  expected: 'true'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 666624262) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_for_efb0c08768bf
  template: "{% for it in record %}{% tablerow node in forloop %}{% endtablerow %}{%
    endfor %}"
  environment:
    record:
      id: '4296'
  expected: |
    <tr class="row1">
    </tr>
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 906027255) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "<tr class=\"row1\">\n<td class=\"col1\"></td></tr>\n".
- name: fuzz_for_f0305bb2fa74
  template: "{% for elem in nil %}{% else %}{% tablerow row in (nil..nil) %}{% endtablerow
    %}{% endfor %}"
  expected: |
    <tr class="row1">
    <td class="col1"></td></tr>
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1487468682) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_for_f7e12d35076e
  template: "{% for entry in (nil..nil) %}{% if nil or nil  [entry.count] %}{% endif
    %}{% endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 937494951) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_for_fd00fcbd39a6
  template: "{% for value in (nil..nil) %}{% for node in b %}{% cycle true %}{% endfor
    %}{% endfor %}"
  environment:
    b: "{% not_a_tag %}"
  expected: 'true'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1248956098) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_id_2f00e98083aa
  template: '{% unless nil %}{% capture user %}{% endcapture %}{% endunless %}{% capture
    user %}{% if "PRICE_START_99_END" and user and user >= user %}{% endif %}{% endcapture
    %}'
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1408561419) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): comparison of String with String failed".
- name: fuzz_id_4855848732ce
  template: "{% capture a %}{% endcapture %}{% if a <= a %}{% endif %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1207310274) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): comparison of String with String failed".
- name: fuzz_id_4875786bffe3
  template: "{% assign item = nums %}{% if false %}{% else %}{% cycle item.url %}{%
    endif %}"
  environment:
    nums: true
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 724950052) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "true".
- name: fuzz_id_5477230a0c96
  template: "{% capture x %}{% endcapture %}{% tablerow item in x[nil] %}{% endtablerow
    %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1327477278) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "<tr class=\"row1\">\n</tr>\n".
- name: fuzz_id_5cfb2101c594
  template: "{% capture entry %}{% endcapture %}{% capture x %}{% endcapture %}{%
    if entry <= x %}{% endif %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 2102527279) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): comparison of String with String failed".
- name: fuzz_id_60be8ded93ec
  template: |-
    {% raw %}a { brace } c
    {% endraw %}{%- assign value = nil -%}
  expected: 'a { brace } c

    '
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1380496619) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "a { brace } c".
- name: fuzz_id_66e979858690
  template: '{% capture y %}{% if nil or c %}{% cycle "Hello, World!92" %}{% endif
    %}{% endcapture %}{{ y }}'
  environment:
    c: []
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 350233435) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Hello, World!92".
- name: fuzz_id_6953f15045f4
  template: "{% if 3.14159 %}{% assign items = true %}{% cycle items.title %}{% endif
    %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1949567550) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "true".
- name: fuzz_id_6b149e1229d5
  template: "{% for it in (nil..nil) %}{% if forloop %}{% assign items = it.title
    %}{% endif %}{% endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1522356006) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_id_755dc6dd6dea
  template: "{% if [\"emoji \U0001F600261\"] and nil and nil %}{% elsif nil %}{% render
    \"header\" %}{% endif %}"
  expected: ''
  complexity: 800
  hint: "Recorded by LiquidIL's differential fuzzer (seed 2067797121) and\nconfirmed
    against reference liquid in a clean subprocess: the expected\noutput is exactly
    what the reference gem renders in strict mode.\nA compiling implementation diverged
    here at recording time,\nproducing: \"Liquid error (line 1): Unknown operator
    \U0001F600261\\\"]\".\n"
- name: fuzz_id_8ac735a54f63
  template: "{% capture data %}{% endcapture %}{% unless 1.5 > data %}{% endunless
    %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1834847803) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): comparison of Float with String failed".
- name: fuzz_id_8b3c34c74afe
  template: '{% if ["Hello, World!719"] contains nil %}{% render "row" %}{% endif
    %}'
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1305141682) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): Unknown operator World!719\"]".
- name: fuzz_id_aab5d715f86b
  template: |-
    {% case total %}{% when nil %}{% decrement total %}{% when nil %}stray % percent
    {% endcase %}
  expected: "-1"
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1234126205) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "-1stray % percent\n".
- name: fuzz_id_ac34f5811b44
  template: "{% for row in (nil..nil) %}{% assign data = row.price %}{% endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1871155386) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_id_c3ab69ca7113
  template: '{% capture list %}{% render "item_partial" %}{% endcapture %}'
  filesystem:
    item_partial: |-
      {% for row in (10..14) reversed limit: 1 %}{% liquid
      echo -1 | uniq | downcase | rstrip
      assign count = -317 | strip_newlines | remove: nil | truncate: value[count.count[false]], forloop.count
      %}{%- if user.last[forloop[forloop]] contains false -%}{{ row[forloop.rindex][1] | truncatewords: true, data[true].price | capitalize | default: false }}{% elsif y[forloop.index] contains row.name and forloop[row][count] and row %}{% elsif 3.14159 != forloop.count.name %}{% else %}{% endif %}{% endfor %}{% for item in (11..21) reversed %}{{ nil | modulo: false | json }}{% endfor %}
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 689471276) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_if_04c1e9cd756a
  template: |-
    {% if false %}{% else %}{% if values < "line1
    line2" %}{% endif %}{% endif %}
  environment:
    values: 0
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1791224530) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): comparison of Integer with String failed".
- name: fuzz_if_05743d548cd7
  template: "{% if names %}{% for elem in names %}{% cycle names.last %}{% endfor
    %}{% endif %}"
  environment:
    names:
      key_54: true
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1062184594) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "{\"key_54\" => true}".
- name: fuzz_if_0787670ec1db
  template: "{% if 0.0 %}{% unless -4611686018427387904 >= x %}{% endunless %}{% endif
    %}"
  environment:
    x: "{{ not_a_tag }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 388455117) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): comparison of Integer with String failed".
- name: fuzz_if_094eee309431
  template: "{% if 1.5 %}{% for elem in (nil..nil) %}{{ forloop }}{% endfor %}{% endif
    %}"
  expected: Liquid::ForloopDrop
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 385318622) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "ForloopDrop".
- name: fuzz_if_0a327d1badef
  template: "{% if nil != false %}{% cycle a[nil] %}{% endif %}"
  environment:
    a: -4611686018427387904
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 2028111530) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "-4611686018427387904".
- name: fuzz_if_0b174324dc09
  template: "{% if 3.14159 %}{% cycle value %}{% endif %}"
  environment:
    value: []
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 131158078) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "[]".
- name: fuzz_if_0c3786a7a2bc
  template: '{% if "  padded  216" <= 0 %}{% endif %}'
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 134285993) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): comparison of String with 0 failed".
- name: fuzz_if_12114cff5754
  template: '{% if 3.14159 >= "nil" %}{% endif %}'
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 411333141) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): comparison of Float with String failed".
- name: fuzz_if_1c7ed1fbe589
  template: "{% if [[\"Hello, World!535\"]] <= nil %}{% for node in nil %}{% for entry
    in nil %}emoji \U0001F600\n{% endfor %}{% endfor %}{% endif %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1988898695) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): Unknown operator World!535\"]]".
- name: fuzz_if_1f4d9e78bce7
  template: '{% if "PRICE_START_99_END788" >= 1 %}{% endif %}'
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1487613379) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): comparison of String with 1 failed".
- name: fuzz_if_22d1589fa72f
  template: "{% if 1.5 %}{% cycle [count] %}{% endif %}"
  environment:
    count: {}
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1314868122) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "{}".
- name: fuzz_if_236212c32af0
  template: "{% if false %}{% else %}{% cycle config %}{% endif %}"
  environment:
    config: []
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1720444481) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "[]".
- name: fuzz_if_29065d4d0bdd
  template: '{% if "{{ not_a_tag }}" < config.first %}{% endif %}'
  environment:
    config:
      name: true
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 2122361472) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): comparison of String with String failed".
- name: fuzz_if_2d18c2ff31ca
  template: "{% if [[\"emoji \U0001F600\"]] != nil or nil %}{{ nil }}{% endif %}"
  expected: ''
  complexity: 800
  hint: "Recorded by LiquidIL's differential fuzzer (seed 970445532) and\nconfirmed
    against reference liquid in a clean subprocess: the expected\noutput is exactly
    what the reference gem renders in strict mode.\nA compiling implementation diverged
    here at recording time,\nproducing: \"Liquid error (line 1): Unknown operator
    \U0001F600\\\"]]\".\n"
- name: fuzz_if_3c014b9a7cf3
  template: '{% if nil or "nil" %}{% cycle y.title %}{% endif %}'
  environment:
    "y": -4611686018427387904
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1283598794) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "-4611686018427387904".
- name: fuzz_if_41f6f2069019
  template: '{% if names < "_S_marker_" or nil %}{% endif %}'
  environment:
    names: "{% not_a_tag %}112"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1236393728) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): comparison of String with String failed".
- name: fuzz_if_4387895b2113
  template: "{% if [\"emoji \U0001F600\"] contains nil %}{% elsif nil %}{{ nil }}{%
    endif %}"
  expected: ''
  complexity: 800
  hint: "Recorded by LiquidIL's differential fuzzer (seed 2036258489) and\nconfirmed
    against reference liquid in a clean subprocess: the expected\noutput is exactly
    what the reference gem renders in strict mode.\nA compiling implementation diverged
    here at recording time,\nproducing: \"Liquid error (line 1): Unknown operator
    \U0001F600\\\"]\".\n"
- name: fuzz_if_462d6ffd551d
  template: '{% if ["{{ not_a_tag }}"] <= nil %}{% case nil %}{% when nil %}{% cycle
    "" %}{% endcase %}{% endif %}'
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1448346185) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): Unknown operator not_a_tag".
- name: fuzz_if_4823586a55c1
  template: '{% if "{{ not_a_tag }}" <= a or nil %}{% endif %}'
  environment:
    a: PRICE_START_99_END273
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1907130465) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): comparison of String with String failed".
- name: fuzz_if_4886f9dda093
  template: |-
    {% if 3.14159 %}{% raw %}
    {% endraw %}{{- nil -}}{% endif %}
  expected: "\n"
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1281834829) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_if_494b2f01e699
  template: "{% if z < z %}{% endif %}"
  environment:
    z: ''
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1518201616) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): comparison of String with String failed".
- name: fuzz_if_4c24367ca398
  template: "{% if false %}{% else %}{% cycle [c] %}{% endif %}"
  environment:
    c: 1.5
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 316995711) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "1.5".
- name: fuzz_if_565dd23ffb85
  template: '{% if false %}{% else %}{% for row in (nil..nil) %}{% cycle ["_S_marker_298"]
    %}{% endfor %}{% endif %}'
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 285495073) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "_S_marker_298".
- name: fuzz_if_5d16d5c85fb9
  template: |-
    {% if "line1
    line2597" >= 3.14159 %}{% endif %}
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 587057752) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): comparison of String with 3.14159 failed".
- name: fuzz_if_5d3730425850
  template: "{% if [\"  padded  \"] contains nil %}{% for entry in nil %}emoji \U0001F600\n{%
    endfor %}{% endif %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1941801537) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): Unknown operator padded".
- name: fuzz_if_5e3700fde659
  template: "{% if entry != nil %}{% cycle [entry] %}{% endif %}"
  environment:
    entry: |-
      line1
      line2
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 792778966) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "line1\nline2".
- name: fuzz_if_635bf0ae0c0e
  template: "{% if false %}{% else %}{% cycle true %}{% endif %}"
  expected: 'true'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 789505130) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_if_63ee48e68a68
  template: '{% if nil or nil or "42284" %}{% cycle entry %}{% endif %}'
  environment:
    entry: []
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1783157084) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "[]".
- name: fuzz_if_6b7c785999bb
  template: '{% if [["{{ not_a_tag }}"]] %}{% for it in nil %}{{ nil }}{% endfor %}{%
    endif %}'
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 415066881) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): Unknown operator not_a_tag".
- name: fuzz_if_7c1f9aa970f2
  template: '{% if ["Hello, World!199"] contains nil %}{% cycle "" %}{% endif %}'
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1009002101) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): Unknown operator World!199\"]".
- name: fuzz_if_7d605c991d00
  template: "{% if false %}{% else %}{% tablerow it in (nil..nil) %}{% endtablerow
    %}{% endif %}"
  expected: |
    <tr class="row1">
    <td class="col1"></td></tr>
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 252319570) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_if_81c0a877b34c
  template: "{% if nil == nil %}{% if true %}{% case nil %}{% when nil, nil %}{{ values
    }}{% endcase %}{% endif %}{% endif %}"
  environment:
    values: {}
  expected: "{}{}"
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1089639399) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "{}".
- name: fuzz_if_86185f79ef8a
  template: "{% if nil != true %}{% cycle true %}{% endif %}"
  expected: 'true'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 590977136) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_if_8c7c4268e524
  template: '{% if "{{ not_a_tag }}520" > 1.5 %}{% endif %}'
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1066381693) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): comparison of String with 1.5 failed".
- name: fuzz_if_8f37897fccef
  template: '{% if ''quote"inside'' < 4611686018427387904 %}{% endif %}'
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1059519335) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): comparison of String with 4611686018427387904 failed".
- name: fuzz_if_952922d52dec
  template: |-
    {% if [["line1
    line2"]] > nil %}café 日本語
    {% endif %}
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1131083962) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): Unknown operator line2\"]]".
- name: fuzz_if_9609181c0982
  template: "{% if record %}{% cycle false %}{% endif %}"
  environment:
    record: {}
  expected: 'false'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 273906547) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_if_962bbb88f050
  template: "{% if data %}{% cycle c[nil] %}{% endif %}"
  environment:
    data: {}
    c: 3.14159
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1730522595) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "3.14159".
- name: fuzz_if_967ae95b4e0d
  template: "{% if true %}{% tablerow node in product[nil] %}{% endtablerow %}{% endif
    %}"
  environment:
    product: '42870'
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1818069977) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "<tr class=\"row1\">\n<td class=\"col1\"></td></tr>\n".
- name: fuzz_if_99afcb1bd2b3
  template: "{% if 0.0 <= list %}{% endif %}"
  environment:
    list: 'false'
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1940102292) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): comparison of Float with String failed".
- name: fuzz_if_a0a158957065
  template: |-
    {% if ["line1
    line2852"] %}{{ nil }}{% endif %}
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1317455337) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): Unknown operator line2852\"]".
- name: fuzz_if_a0e4524f111f
  template: '{% if ["  padded  "] contains nil and nil %}{% tablerow entry in nil
    %}{% continue %}{% endtablerow %}{% endif %}'
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1541939726) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): Unknown operator padded".
- name: fuzz_if_ad5c84a1cb1a
  template: "{% if names > 1 %}{% endif %}"
  environment:
    names: 'false'
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 424186259) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): comparison of String with 1 failed".
- name: fuzz_if_b4dd664923db
  template: '{% if user <= "false" %}{% endif %}'
  environment:
    user: Hello, World!
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1019462399) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): comparison of String with String failed".
- name: fuzz_if_b67d82d28167
  template: "{% if names %}{% cycle true %}{% endif %}"
  environment:
    names: {}
  expected: 'true'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 782021000) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_if_bafdc4aedbf4
  template: "{% if nil or b or nil %}{% cycle false %}{% endif %}"
  environment:
    b: 4611686018427387904
  expected: 'false'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1497625048) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_if_bf1960e5d46f
  template: '{% if ["Hello, World!"] == nil %}{% for it in nil %}{{ nil }}{% endfor
    %}{% endif %}'
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1919768814) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): Unknown operator World!\"]".
- name: fuzz_if_c0a6e004cae9
  template: "{% if nil == nil or nil %}{% for row in (nil..nil) %}{% unless nil %}{%
    cycle [names] %}{% endunless %}{% endfor %}{% endif %}"
  environment:
    names: []
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1685100048) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "[]".
- name: fuzz_if_cb0d2f8ab7b6
  template: |-
    {% if [["line1
    line2"]] contains nil %}{% elsif nil %}{% for row in nil %}{% if nil %}{% echo nil %}{% endif %}{% endfor %}{% endif %}
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 332830335) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): Unknown operator line2\"]]".
- name: fuzz_if_d6597f94d8dd
  template: |-
    {% if 0 > "line1
    line2" %}{% endif %}
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1383583661) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): comparison of Integer with String failed".
- name: fuzz_if_e2a197cf172c
  template: |-
    {% if false %}{% else %}{% case nil %}{% when nil, nil %}{% raw %}
    {% endraw %}{% endcase %}{% endif %}
  expected: "\n\n"
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1277220941) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "\n".
- name: fuzz_if_e3ce6baf6527
  template: '{% if [[[["{{ not_a_tag }}"]]]] < nil %}{% cycle "" %}{% endif %}'
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1707577364) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): Unknown operator not_a_tag".
- name: fuzz_if_e88b94573776
  template: "{% if 1.5 %}{% unless nil %}{% cycle info %}{% endunless %}{% endif %}"
  environment:
    info: []
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1055291520) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "[]".
- name: fuzz_if_ecead1f12236
  template: "{% if a %}{% cycle a %}{% endif %}"
  environment:
    a: []
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 758930109) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "[]".
- name: fuzz_if_edca88f0ac4a
  template: "{% if false %}{% else %}{% raw %}emoji \U0001F600\n{% endraw %}{%- for
    item in nil -%}{% endfor %}{% endif %}"
  expected: "emoji \U0001F600\n"
  complexity: 800
  hint: "Recorded by LiquidIL's differential fuzzer (seed 103483827) and\nconfirmed
    against reference liquid in a clean subprocess: the expected\noutput is exactly
    what the reference gem renders in strict mode.\nA compiling implementation diverged
    here at recording time,\nproducing: \"emoji \U0001F600\".\n"
- name: fuzz_if_efadb326a6aa
  template: '{% if "café" > product %}{% endif %}'
  environment:
    product: 0
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 122460401) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): comparison of String with 0 failed".
- name: fuzz_if_f0f42e3e9e82
  template: '{% if [["Hello, World!101"]] contains nil %}{% elsif nil %}{{ nil }}{%
    endif %}'
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1946198768) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): Unknown operator World!101\"]]".
- name: fuzz_if_f199c3c253e0
  template: "{% if user >= user %}{% for node in (nil..nil) %}{% case nil %}{% when
    node.count %}{% endcase %}{% endfor %}{% endif %}"
  environment:
    user: -4611686018427387904
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 84747081) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_if_f524c5055a5b
  template: "{% if false %}{% else %}{% cycle user.size %}{% endif %}"
  environment:
    user: 3.14159
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 147722757) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "3.14159".
- name: fuzz_if_f6adbfd2975e
  template: "{% if b %}{% if values %}{% cycle true %}{% endif %}{% endif %}"
  environment:
    values: a,b,c,d792
    b: -4611686018427387904
  expected: 'true'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 59089457) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_if_f81d748b11bd
  template: "{% if nil != true %}{% for entry in (nil..nil) %}{% cycle .count.url
    %}{% endfor %}{% endif %}"
  environment:
    count: -0.5
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1147927097) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "-0.5".
- name: fuzz_if_fd3a33786a09
  template: "{% if -0.5 >= total %}{% endif %}"
  environment:
    total: "{% not_a_tag %}345"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 2059694684) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): comparison of Float with String failed".
- name: fuzz_if_ff2fa8f35f5d
  template: "{% if item >= item %}{% endif %}"
  environment:
    item: _S_marker_804
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1820699949) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): comparison of String with String failed".
- name: fuzz_join_3f720a2e5943
  template: "{{ nil | truncatewords: nil | join }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 2080757914) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_join_6c0f5313daee
  template: "{% unless nil %}{{ nil | truncatewords: nil | join }}{% endunless %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 60969826) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_join_87c8c7098003
  template: "{{ nil | json | join }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1047906372) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "null".
- name: fuzz_join_b6b07c16a9c9
  template: "{{ nil | truncate: nil | compact | join }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1020344903) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_join_bd06d3c8b75c
  template: "{% capture values %}{% for entry in (nil..nil) %}{{ [entry.count] | modulo
    | join }}{% endfor %}{% endcapture %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1477624301) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_join_bdec0bc4603c
  template: "{% echo nil | truncatewords: nil | join %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 314970395) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_json_3166b38d76e1
  template: "{% for row in (nil..nil) %}{{ nil | json }}{% endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 217488421) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "null".
- name: fuzz_json_35db9f49f693
  template: "{% case nil %}{% when nil %}{{ nil | json }}{% endcase %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1356063251) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "null".
- name: fuzz_json_455c5752f873
  template: |-
    {% assign a = nil | json %}{% for row in a %}stray { brace
    {% endfor %}
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1122618194) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "stray { brace\n".
- name: fuzz_json_5acd71aad879
  template: "{% assign count = nil | json %}{{ count }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1316135478) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "null".
- name: fuzz_json_89bd3e2f8a8b
  template: "{% if false %}{% else %}{{ nil | json }}{% endif %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 4212643) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "null".
- name: fuzz_json_ac3ad491f2e5
  template: "{% echo nil | json %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1947760739) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "null".
- name: fuzz_json_bfe86671765f
  template: |-
    {% liquid
    echo nil | json
    %}
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1744317216) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "null".
- name: fuzz_json_c234d1eb3922
  template: "{{ nil | json }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 551344287) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "null".
- name: fuzz_json_ce790ca1945c
  template: "{% for elem in nil %}{% else %}{{ nil | json }}{% endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 867980117) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "null".
- name: fuzz_json_dda61b17b8f0
  template: "{% for elem in (nil..nil) %}{% assign entry = elem.title | json %}{%
    endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 30972224) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_json_deec0e3bf958
  template: "{% if nil or nil != data %}{{ nil | json }}{% endif %}"
  environment:
    data: -0.5
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1355086734) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "null".
- name: fuzz_json_fd3c756b2c2c
  template: "{% for item in (nil..nil) %}{% echo nil | json %}{% endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 9525547) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "null".
- name: fuzz_last_1e1c9e77f098
  template: "{% capture nums %}{{ count | strip | last }}{% endcapture %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1348613469) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of nil into String".
- name: fuzz_last_dfdceb013eca
  template: "{{ nil | truncate: nil | last }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1816531792) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_lstrip_967bc5d8e5e6
  template: "{{ nil | truncatewords: nil | lstrip }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1855338542) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_lstrip_a5fdb2e392a6
  template: "{% for item in (nil..nil) %}{{ nil | json | lstrip }}{% endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 419269371) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "null".
- name: fuzz_lstrip_c08ac5762c96
  template: "{% echo nil | json | lstrip %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 3108773) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "null".
- name: fuzz_misc_0c7454a2bc39
  template: '{% for value in nil %}{% case "{% not_a_tag %}" %}{% endcase %}{% endfor
    %}'
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 45894360) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
- name: fuzz_misc_ac945a73e868
  template: "{{ nil | divided_by }}"
  render_errors: true
  expected: 'Liquid error: wrong number of arguments (given 1, expected 2)'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 2127868193) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): wrong number of arguments (given 1, expected 2)".
- name: fuzz_raw_6df9451ad69d
  template: "tab\there\n{% comment %}{% endcomment %}{%- if nil -%}{% endif %}"
  expected: "tab\there\n"
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 510837209) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "tab\there".
- name: fuzz_raw_tag_2e5c087e68af
  template: |-
    {% raw %}
    {% endraw %}{%- echo nil -%}
  expected: "\n"
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1218682849) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_raw_tag_5905d1c77dd7
  template: |-
    {% raw %}café 日本語
    {% endraw %}{%- for elem in nil -%}{% endfor %}
  expected: 'café 日本語

    '
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 111833731) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "café 日本語".
- name: fuzz_raw_tag_919b8783bb82
  template: "{% raw %}tab\there\n{% endraw %}{%- if nil -%}{% endif %}"
  expected: "tab\there\n"
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1290051317) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "tab\there".
- name: fuzz_raw_tag_9b7d756705b5
  template: |-
    {% raw %}stray { brace
    {% endraw %}{%- if false -%}{% else %}{% endif %}
  expected: 'stray { brace

    '
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1872193455) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "stray { brace".
- name: fuzz_raw_tag_cf7682433b5f
  template: "{% raw %}  {% endraw %}{{- nil -}}"
  expected: "  "
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1263472168) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_remove_3c1a9d17b7e0
  template: "{% assign flag = nil | remove %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 440970924) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): wrong number of arguments (given 1, expected 2)".
- name: fuzz_reverse_17a16d6559f1
  template: "{% unless nil %}{{ nil | truncatewords: nil | reverse }}{% endunless
    %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1620337159) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_reverse_3f6e82ba0080
  template: "{{ nil | truncatewords: nil | reverse }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 735151541) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_round_1855a26bd7f6
  template: "{{ nums | lstrip | round }}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1267567175) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "\u0000".
- name: fuzz_round_693a5d6f470d
  template: |-
    {% case nil %}{% when nil, nil %}{% liquid
    echo nil | round
    %}{% endcase %}
  expected: '00'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 959659585) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "0".
- name: fuzz_round_805a4381eaab
  template: "{{ flag | rstrip | round }}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 159119880) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "\u0000".
- name: fuzz_round_a02956e1b922
  template: "{{ nil | truncatewords: nil | round }}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 227443782) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_round_accd381a92bf
  template: "{{ b | upcase | round }}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1411517206) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "\u0000".
- name: fuzz_round_ccb5d36a28fc
  template: "{% case nil %}{% when nil, nil %}{{ nil | round }}{% endcase %}"
  expected: '00'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 797809941) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "0".
- name: fuzz_round_f29cf61ff444
  template: "{{ nil | split | upcase | round }}"
  render_errors: true
  expected: 'Liquid error: wrong number of arguments (given 1, expected 2)'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 770673401) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "\u0000".
- name: fuzz_round_f69f01b0cdb9
  template: "{% for node in nil %}{% else %}{{ nil | truncate: nil | round }}{% endfor
    %}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 660237212) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_rstrip_7b8fefd47179
  template: "{{ nil | truncatewords: nil | rstrip }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1553753532) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_rstrip_89991ba3cd5b
  template: "{% echo nil | truncate: nil | rstrip %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 83643379) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_size_12e83fd0f280
  template: "{{ nil | last | rstrip | size }}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 664089276) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "\u0000".
- name: fuzz_size_1ca0a9e8a134
  template: "{{ config | lstrip | size }}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 856432456) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "\u0000".
- name: fuzz_size_2af09de708c2
  template: "{% echo names | lstrip | size %}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1901083088) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "\u0000".
- name: fuzz_size_34a8bff8bde7
  template: "{% if record %}{% echo nil | truncatewords: nil | size %}{% endif %}"
  environment:
    record: []
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 572958352) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_size_474706408958
  template: "{{ nil | json | size }}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1002702255) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "4".
- name: fuzz_size_499d5f8bc23e
  template: "{{ nil | truncatewords: nil | size }}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 2127884871) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_size_964c3e02ad85
  template: "{% unless nil %}{{ nil | truncate: nil | size }}{% endunless %}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 702205351) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_size_9bdff3969a3a
  template: "{% echo item | capitalize | size %}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 438048459) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "\u0000".
- name: fuzz_size_aa0477e07156
  template: "{{ nil | truncate: nil | size }}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 249255389) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_size_b296bfdd8211
  template: "{% for row in (nil..nil) %}{% echo nil | join | lstrip | size %}{% endfor
    %}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 55250430) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "\u0000".
- name: fuzz_size_d14ccf4d888d
  template: |-
    {% liquid
    echo nil | json | size
    %}
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1695865158) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "4".
- name: fuzz_slice_badcfcf0e762
  template: "{% for row in (nil..nil) %}{% assign y = nil | slice: row.title %}{%
    endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1128367372) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_sort_182e3cf6b3d1
  template: "{{ nil | truncatewords: nil | sort }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1899761931) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_sort_366d707f16fd
  template: "{% case nil %}{% when nil %}{% echo nil | json | capitalize | sort %}{%
    endcase %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1647990855) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Null".
- name: fuzz_sort_4225cdb2ad39
  template: "{% case nil %}{% else %}{{ nil | json | sort }}{% endcase %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 173992544) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "null".
- name: fuzz_sort_a5223b0ae001
  template: |-
    {% unless nil %}{% liquid
    assign z = nil | sort
    %}{% endunless %}{% cycle z %}
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 706754130) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "[]".
- name: fuzz_sort_bbad8f80d2f3
  template: "{% for entry in (nil..nil) %}{{ forloop | sort }}{% endfor %}"
  expected: Liquid::ForloopDrop
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 247102805) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "ForloopDrop".
- name: fuzz_sort_natural_00f1f636c21e
  template: "{{ nil | truncatewords: nil | escape | sort_natural }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 390829198) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_sort_natural_4b8d7da8892d
  template: "{% echo nil | truncate: nil | sort_natural %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1298127430) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_sort_natural_9bd2d8abb8bc
  template: "{{ entry | escape_once | round | sort_natural }}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1761323194) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "[0]".
- name: fuzz_sort_natural_a31d705d0922
  template: "{{ values | floor | floor | sort_natural }}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1342080029) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "[0]".
- name: fuzz_sort_natural_cf9671254bbe
  template: |-
    {% liquid
    echo names.first | sort_natural
    %}
  environment:
    names:
      a:
        id: {}
  expected: a{"id"=>{}}
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1093088487) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "a{\"id\" => {}}".
- name: fuzz_split_15610f8411d9
  template: "{% case nil %}{% when nil %}{% echo nil | split %}{% endcase %}"
  render_errors: true
  expected: 'Liquid error: wrong number of arguments (given 1, expected 2)'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 370667747) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_split_72f30ba7fdd6
  template: "{% unless nil %}{{ nil | split }}{% endunless %}"
  render_errors: true
  expected: 'Liquid error: wrong number of arguments (given 1, expected 2)'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 919045482) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_split_75d9b39e869e
  template: "{% for value in (nil..nil) %}{{ nil | split }}{% endfor %}"
  render_errors: true
  expected: 'Liquid error: wrong number of arguments (given 1, expected 2)'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1649847870) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_split_abf3719442ca
  template: |-
    {% liquid
    echo nil | split
    %}
  render_errors: true
  expected: 'Liquid error: wrong number of arguments (given 1, expected 2)'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 611593061) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_split_afcfd594708f
  template: "{{ nil | split }}"
  render_errors: true
  expected: 'Liquid error: wrong number of arguments (given 1, expected 2)'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1990268906) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_split_b99629ada9ef
  template: |-
    {% if "hello" %}{% liquid
    echo nil | split
    %}{% endif %}
  render_errors: true
  expected: 'Liquid error: wrong number of arguments (given 1, expected 2)'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1898324061) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_split_ceab79419334
  template: |-
    {% for elem in nil %}{% else %}{% liquid
    echo nil | split
    %}{% endfor %}
  render_errors: true
  expected: 'Liquid error: wrong number of arguments (given 1, expected 2)'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 26729653) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_split_d36bf2c3b15e
  template: |-
    {% for entry in (nil..nil) %}{% liquid
    assign info = entry.title | split
    %}{% endfor %}
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 643738137) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_split_e2a34ac258d1
  template: "{% echo nil | split %}"
  render_errors: true
  expected: 'Liquid error: wrong number of arguments (given 1, expected 2)'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 594491212) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_split_e7795649ef09
  template: "{% assign info = nil | split %}{% cycle info %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1834596990) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "[]".
- name: fuzz_strip_a76f64edebf8
  template: "{% assign data = nil | strip %}{% if x >= data %}{% endif %}"
  environment:
    x: 0.0
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1538098751) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): comparison of Float with String failed".
- name: fuzz_strip_c1e5985592fc
  template: "{% if flag %}{% echo nil | truncate: nil | strip %}{% endif %}"
  environment:
    flag: -1
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1622567319) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_strip_d3d2506c9a01
  template: '{% if ["Hello, World!"] contains nil %}{% capture items %}{% endcapture
    %}{% endif %}{% if items %}{{ nil | prepend | strip }}{% endif %}'
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1482171003) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): wrong number of arguments (given 1, expected 2)".
- name: fuzz_strip_fda8566733ce
  template: "{{ nil | json | strip }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 2059345290) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "null".
- name: fuzz_strip_newlines_897f19875901
  template: "{% if false %}{% else %}{{ nil | truncate: nil | strip_newlines }}{%
    endif %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1003629963) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_strip_newlines_a10d25bd2140
  template: "{{ nil | json | strip_newlines }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 916782878) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "null".
- name: fuzz_strip_newlines_aa59bb0ee867
  template: "{{ nil | truncate: nil | strip_newlines }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 2052741008) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_tablerow_218f5ddd1884
  template: "{% tablerow it in (nil..nil) %}{% endtablerow %}"
  expected: |
    <tr class="row1">
    <td class="col1"></td></tr>
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 962420006) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_tablerow_9f3e95552412
  template: "{% tablerow row in [nil] %}{% endtablerow %}"
  environment:
    items: 1.5
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 488615282) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "<tr class=\"row1\">\n</tr>\n".
- name: fuzz_tablerow_c3e1ce628c39
  template: "{% tablerow row in entry[nil] %}{% endtablerow %}"
  environment:
    entry: []
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1464041841) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "<tr class=\"row1\">\n</tr>\n".
- name: fuzz_tablerow_d1bf686d83d8
  template: "{% tablerow it in count.first %}{% endtablerow %}"
  environment:
    count:
      a: []
  expected: |
    <tr class="row1">
    <td class="col1"></td><td class="col2"></td></tr>
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 2101702347) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "<tr class=\"row1\">\n<td class=\"col1\"></td></tr>\n".
- name: fuzz_times_29f9eab41cc1
  template: "{% assign flag = nil | times %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1521417249) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): wrong number of arguments (given 1, expected 2)".
- name: fuzz_truncate_0b94d62b357b
  template: '{% if "日本語こんにちは" %}{{ nil | truncate: nil }}{% endif %}'
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 897880346) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_truncate_0e4d618afc64
  template: "{% for node in config %}{% echo nil | truncate: nil %}{% endfor %}"
  environment:
    config:
      b:
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 826898565) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_truncate_1e9de1ca3eb6
  template: "{% for entry in (nil..nil) %}{% echo nil | truncate: nil %}{% endfor
    %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 2000992590) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_truncate_1fcabf679d44
  template: |-
    {% if false %}{% else %}{% if "a,b,c,d" %}{% liquid
    echo nil | truncate: nil
    %}{% endif %}{% endif %}
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 333743071) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_truncate_2863b5559668
  template: "{% case nil %}{% when nil %}{{ nil | truncate: nil }}{% endcase %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 375212868) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_truncate_40917e95611a
  template: "{% if 1.5 %}{{ nil | truncate: nil }}{% endif %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 205108047) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_truncate_458fd2794ea4
  template: "{% for row in (nil..nil) %}{% assign c = nil | truncate: row.id %}{%
    endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1247193757) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_truncate_4b4ecb1aedef
  template: "{% if true and nil or \"emoji \U0001F600169\" %}{% echo nil | truncate:
    nil %}{% endif %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1709984599) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_truncate_5cbd5eaa9ca5
  template: "{% for elem in nil %}{% else %}{{ nil | truncate: nil }}{% endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 401023730) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_truncate_76d8c4ca8f60
  template: "{% echo nil | truncate: nil %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 2101297685) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_truncate_79e2d347f412
  template: "{% for value in (nil..nil) %}{{ nil | truncate: nil }}{% endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1562939475) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_truncate_807d54b5e2c8
  template: "{% if 0 %}{{ nil | truncate: nil }}{% endif %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1962930112) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_truncate_b10a68a8bc9a
  template: "{% case nil %}{% when nil %}{% unless nil %}{{ nil | truncate: nil }}{%
    endunless %}{% endcase %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1616641286) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_truncate_b155158c279a
  template: "{% if false %}{% else %}{{ nil | truncate: nil }}{% endif %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1461605730) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_truncate_c1b4148b8ebe
  template: |-
    {% liquid
    echo nil | truncate: nil
    %}
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 559224568) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_truncate_d40493133a1e
  template: "{% if false %}{% else %}{% tablerow it in names %}{% echo nil | truncate:
    nil %}{% endtablerow %}{% endif %}"
  environment:
    names:
      id: []
  expected: |
    <tr class="row1">
    <td class="col1"></td></tr>
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 2076627335) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "<tr class=\"row1\">\n<td class=\"col1\">Liquid error (line 1): invalid integer</td></tr>\n".
- name: fuzz_truncate_de50829c143c
  template: "{% unless nil %}{{ nil | truncate: nil }}{% endunless %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1640498161) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_truncate_e301921bb8dc
  template: "{% if nil or nil or true %}{{ nil | truncate: nil }}{% endif %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1818614454) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_truncate_e705b53f762e
  template: "{{ nil | truncate: nil }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 51789807) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_truncate_e81b4a71c1e6
  template: "{% if false != nil %}{{ nil | truncate: nil }}{% endif %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1996532236) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_truncatewords_00280d562f3a
  template: "{% if 3.14159 %}{% echo nil | truncatewords: nil %}{% endif %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 445250549) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_truncatewords_05a88e6276a9
  template: "{% unless nil %}{{ nil | truncatewords: nil }}{% endunless %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1677856525) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_truncatewords_30fb2e4df300
  template: "{% for entry in (nil..nil) %}{% for node in forloop %}{% echo nil | truncatewords:
    nil %}{% endfor %}{% endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 108924910) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_truncatewords_42bb094366a1
  template: "{% for node in nil %}{% else %}{{ nil | truncatewords: nil }}{% endfor
    %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 198311365) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_truncatewords_430d8a28f8fa
  template: "{% case nil %}{% when nil %}{{ nil | truncatewords: nil }}{% endcase
    %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1053224609) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_truncatewords_4e01b7fe0f42
  template: "{% tablerow row in names %}{{ nil | truncatewords: nil }}{% endtablerow
    %}"
  environment:
    names:
    - true
  expected: |
    <tr class="row1">
    <td class="col1"></td></tr>
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 211968790) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "<tr class=\"row1\">\n<td class=\"col1\">Liquid error (line 1): invalid integer</td></tr>\n".
- name: fuzz_truncatewords_4f434186fdd5
  template: "{% case nil %}{% else %}{{ nil | truncatewords: nil }}{% endcase %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 868321878) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_truncatewords_57f4558afa93
  template: "{{ nil | truncatewords: nil }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 510113249) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_truncatewords_6748be03ec77
  template: "{% echo nil | truncatewords: nil %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 2020010394) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_truncatewords_73936022320d
  template: "{% if flag %}{{ nil | truncatewords: nil }}{% endif %}"
  environment:
    flag: true
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1404683500) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_truncatewords_97cd61471d00
  template: '{% if true and "nil" and names %}{{ nil | truncatewords: nil }}{% endif
    %}'
  environment:
    names: -0.5
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 499358676) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_truncatewords_9b1793600bb4
  template: "{% unless nil %}{% echo nil | truncatewords: nil %}{% endunless %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 2065946485) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_truncatewords_ab532a1802cb
  template: "{% for elem in list %}{% echo nil | truncatewords: nil %}{% endfor %}"
  environment:
    list:
      PRICE_START: -4611686018427387904
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 840592380) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_truncatewords_d355b9673bbf
  template: "{% if false %}{% else %}{{ nil | truncatewords: nil }}{% endif %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1712445476) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_truncatewords_f614d955c787
  template: "{% if false %}{% else %}{% unless nil %}{{ nil | truncatewords: nil }}{%
    endunless %}{% endif %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1472973266) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_truncatewords_ff5d8f57c2bb
  template: |-
    {% liquid
    echo nil | truncatewords: nil
    %}
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1889755001) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_uniq_04734f69fde7
  template: "{{ values | ceil | default | uniq }}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1001727266) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "[0]".
- name: fuzz_uniq_3e7afd0ee8fb
  template: |-
    {% liquid
    assign x = nil | uniq
    %}{% cycle x %}
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 72962226) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "[]".
- name: fuzz_uniq_7c934a5b0fb0
  template: "{{ data | json | abs | uniq }}"
  environment:
    data:
      PRICE_START: -0.5
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 461386185) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "0.0".
- name: fuzz_uniq_a778b8b81ab9
  template: "{{ x | ceil | uniq }}"
  expected: '0'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 445956435) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "[0]".
- name: fuzz_uniq_ae7a716a7266
  template: "{{ nil | truncate: nil | uniq }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1473459340) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_unless_174079ec64bd
  template: |-
    {% unless nil %}{% case nil %}{% when nil, nil %}Hello, World!
    {% endcase %}{% endunless %}
  expected: |
    Hello, World!
    Hello, World!
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1090517462) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Hello, World!\n".
- name: fuzz_unless_19b944f46738
  template: "{% unless nil %}{% cycle data.last %}{% endunless %}"
  environment:
    data: -4611686018427387904
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 904400593) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "-4611686018427387904".
- name: fuzz_unless_1f5c6f919070
  template: "{% unless nil %}{% cycle false %}{% endunless %}"
  expected: 'false'
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1876122733) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_unless_2222ff3fa000
  template: '{% unless nil != total %}{% else %}{% case nil %}{% when "{% not_a_tag
    %}" %}{% endcase %}{% endunless %}'
  environment:
    total: true
  expected: "\" %}"
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1345283639) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_unless_4b65d41f85e0
  template: "{% unless x > 3.14159 %}{% endunless %}"
  environment:
    x: "{{ not_a_tag }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1512070320) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): comparison of String with 3.14159 failed".
- name: fuzz_unless_665a17b4d4c0
  template: "{% unless nil %}{% cycle [a] %}{% endunless %}"
  environment:
    a: "  padded  "
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1685596383) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "  padded  ".
- name: fuzz_unless_8d61c346b6d7
  template: "{% unless nil %}{% cycle [-0.5] %}{% endunless %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1337514529) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "-0.5".
- name: fuzz_unless_a365b216e4b1
  template: "{% unless nil %}{% for entry in (nil..nil) %}{% cycle forloop %}{% endfor
    %}{% endunless %}"
  expected: Liquid::ForloopDrop
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 194425524) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "ForloopDrop".
- name: fuzz_unless_ba4d6c3dbd86
  template: "{% unless nil %}{% cycle nums %}{% endunless %}"
  environment:
    nums: []
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 330041295) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "[]".
- name: fuzz_unless_c084a4a03910
  template: "{% unless nil %}{% for value in (nil..nil) %}{% cycle value.id %}{% endfor
    %}{% endunless %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 465308085) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "0".
- name: fuzz_unless_c2d8792b29e6
  template: "{% unless nil %}{% for node in user %}{% unless nil contains node.id
    %}{% endunless %}{% endfor %}{% endunless %}"
  environment:
    user:
    - -226
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 970669407) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): no implicit conversion of String into Integer".
- name: fuzz_unless_d34f55f106e1
  template: '{% unless nil %}{% case nil %}{% when "{% not_a_tag %}" %}{% endcase
    %}{% endunless %}'
  expected: "\" %}"
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1814899297) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "".
- name: fuzz_unless_fa97fb82bfa5
  template: '{% unless nil %}{% cycle "a,b,c,d" %}{% endunless %}'
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1332642897) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "a,b,c,d".
- name: fuzz_unless_fefb34804d54
  template: "{% unless nil %}{% if list <= -65 %}{% endif %}{% endunless %}"
  environment:
    list: quote"inside
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1347407157) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): comparison of String with -65 failed".
- name: fuzz_upcase_31d1b01fb0b1
  template: "{% for it in (nil..nil) %}{{ nil | truncate: nil | upcase }}{% endfor
    %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1694181752) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_upcase_34e54ad8d242
  template: "{{ nil | truncatewords: nil | upcase }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 947233700) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_upcase_7993a044483c
  template: "{% for elem in (nil..nil) %}{{ nil | json | upcase }}{% endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1979879971) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "NULL".
- name: fuzz_upcase_e81dbe1bd250
  template: "{% case nil %}{% when nil, nil %}{{ nil | sort_natural | upcase }}{%
    endcase %}"
  expected: "[][]"
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 2032299187) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "[]".
- name: fuzz_url_decode_32be235e86df
  template: "{{ nil | truncate: nil | url_decode }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1205294514) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_url_decode_7dfaa012a1b4
  template: "{% for value in (nil..nil) %}{{ nil | truncate: nil | url_decode }}{%
    endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 806299091) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_url_decode_b78e19866dd8
  template: "{% if false %}{% else %}{{ nil | truncate: nil | url_decode }}{% endif
    %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 2054411353) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_url_encode_76a1681f2b5a
  template: "{{ nil | truncate: nil | url_encode }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 1528012111) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_url_encode_7d77d2e3e816
  template: "{{ nil | truncatewords: nil | url_encode }}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 891247298) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
- name: fuzz_url_encode_87c3f17af887
  template: "{% for node in (nil..nil) %}{{ nil | truncate: nil | url_encode }}{%
    endfor %}"
  expected: ''
  complexity: 800
  hint: |
    Recorded by LiquidIL's differential fuzzer (seed 71196827) and
    confirmed against reference liquid in a clean subprocess: the expected
    output is exactly what the reference gem renders in strict mode.
    A compiling implementation diverged here at recording time,
    producing: "Liquid error (line 1): invalid integer".
